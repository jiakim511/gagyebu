# 가계부 앱 설치 가이드 (GitHub Pages + Apps Script)

## 이번에 또 바뀐 것

이전 방식(Apps Script가 앱 화면까지 통째로 서빙)에서는 홈 화면에 추가해도 커스텀 아이콘이 안 먹고 글자 아이콘('가')만 뜨는 문제가 있었습니다. 원인은 Apps Script가 모든 웹앱 화면을 iframe 안에 넣어서 서빙하기 때문에, 아이콘 설정을 iOS가 읽을 수 없는 구조였던 것입니다 (제 코드 문제가 아니라 Apps Script 자체의 제약입니다).

그래서 역할을 나눴습니다.

- **앱 화면(가계부-app.html)**: GitHub Pages라는 무료 정적 호스팅에 올립니다. 진짜 최상위 https 페이지라서 아이콘도 정상 동작하고 속도도 더 빠릅니다.
- **데이터 서버(Code.gs)**: 계속 Google Apps Script에 둡니다. 카드 결제 문자를 받아서 저장하고, 앱이 그 내역을 읽고 확정 처리하는 API 역할만 합니다.

앱과 데이터 서버가 서로 다른 주소이기 때문에, 앱에 "연동 설정" 화면이 새로 생겼습니다 — Apps Script를 배포하면 나오는 URL과 토큰을 딱 한 번 앱에 입력해두면 그 뒤로는 자동으로 동기화됩니다.

---

## 1단계. 구글 시트 + Apps Script 프로젝트 만들기 (데이터 서버)

1. [sheets.google.com](https://sheets.google.com)에서 새 스프레드시트를 만듭니다. 이름은 자유입니다 (예: "가계부 웹훅").
2. 메뉴에서 **확장 프로그램 → Apps Script**를 클릭합니다.
3. 편집기 왼쪽에 기본으로 있는 `Code.gs`(또는 `코드.gs`) 파일 내용을 전부 지우고, 함께 드린 `Code.gs` 내용을 그대로 붙여넣습니다.
4. 코드 위쪽의 이 줄을 찾아서 값을 바꿉니다.

   ```
   var SECRET_TOKEN = '여기에-나만-아는-임의의-문자열-입력';
   ```

   아무도 못 맞출 문자열로 바꿔주세요 (예: `sh-9f27x-mybudget-2026`). 특수문자 없이 영문·숫자·하이픈만 쓰는 걸 추천합니다. **이 값을 나중에 앱의 "연동 설정"에도 똑같이 입력해야 하니 잘 기억해두세요.**
5. 저장(Ctrl+S / Cmd+S)합니다. (이번에는 HTML 파일을 따로 만들 필요가 없습니다 — Index.html은 이제 안 씁니다.)

## 2단계. Apps Script를 웹앱으로 배포하기 (데이터 서버 주소 받기)

1. 편집기 오른쪽 위 **배포 → 새 배포**를 클릭합니다.
2. 유형 선택(⚙️ 아이콘)에서 **웹앱**을 선택합니다.
3. 설정:
   - 실행할 사용자: **나(Me)**
   - 액세스 권한: **모든 사용자(Anyone)**
4. **배포**를 클릭하면 구글 계정 권한 승인 화면이 나옵니다. "고급" → "안전하지 않음(프로젝트명)으로 이동" 순서로 눌러서 승인해줍니다 (본인이 만든 스크립트라 안전합니다).
5. 배포가 끝나면 `https://script.google.com/macros/s/xxxxxxxx/exec` 형태의 URL이 나옵니다. **이 URL이 데이터 서버 주소입니다.** 복사해두세요 (5단계에서 앱에 입력합니다).
6. 브라우저에서 그 URL을 그냥 한 번 열어보세요. "가계부 웹훅 서버가 정상 동작 중입니다."라는 문구가 뜨면 정상입니다.
7. 나중에 `Code.gs`를 수정하면, **배포 → 배포 관리 → 수정(연필 아이콘) → 새 버전 → 배포**를 다시 해야 수정 내용이 실제 URL에 반영됩니다.

## 3단계. GitHub Pages에 앱 화면 올리기 (설치 없이 웹 화면에서 전부 가능합니다)

1. [github.com](https://github.com)에 접속해서 계정이 없으면 **Sign up**으로 무료 계정을 만듭니다. 이미 있으면 로그인합니다.
2. 오른쪽 위 **+ → New repository**를 클릭합니다.
3. Repository name에 원하는 이름을 입력합니다 (예: `my-gagyebu`). Public/Private 아무거나 상관없습니다 (Private도 GitHub Pages 무료로 됩니다). **Create repository**를 클릭합니다.
4. 만들어진 빈 저장소 페이지에서 **uploading an existing file** (또는 `Add file → Upload files`)을 클릭합니다.
5. 함께 드린 `gagyebu-app.html`과 `manifest.json` 두 파일을 그 화면에 끌어다 놓습니다. **`gagyebu-app.html`의 파일명은 반드시 `index.html`로 바꿔주세요** (업로드 화면에서 파일명을 클릭하면 수정할 수 있습니다 — `index.html`이어야 주소창에 파일명 없이 바로 열립니다). `manifest.json`은 이름 그대로 올리면 됩니다 (아이폰이 이 앱을 "제대로 설치된 웹앱"으로 인식하는 데 도움이 됩니다).
6. 아래 **Commit changes**를 클릭해서 저장합니다.
7. 저장소 위 메뉴에서 **Settings**를 클릭합니다.
8. 왼쪽 메뉴에서 **Pages**를 클릭합니다.
9. **Build and deployment → Source**를 **Deploy from a branch**로 두고, **Branch**를 `main`(또는 `master`) / `/(root)`로 선택한 뒤 **Save**를 클릭합니다.
10. 1~2분 기다린 뒤 같은 화면을 새로고침하면 초록색으로 `Your site is live at https://아이디.github.io/저장소이름/` 같은 주소가 나옵니다. **이 주소가 이제부터 여러분이 실제로 쓸 가계부 앱 주소입니다.**

## 4단계. 아이폰에 설치하기

1. 아이폰 Safari에서 3단계 마지막에 받은 `https://아이디.github.io/저장소이름/` 주소로 접속합니다. 가계부 앱 화면이 뜨면 성공입니다.
2. Safari 하단(또는 상단)의 공유 아이콘을 누르고 **홈 화면에 추가**를 선택합니다.
3. 추가하면 홈 화면에 이번엔 제대로 커스텀 아이콘이 뜨고, 탭하면 주소창 없이 앱처럼 전체화면으로 열립니다.

## 5단계. 앱에서 연동 설정하기 (데이터 서버와 연결)

1. 앱 하단 **확인함** 탭으로 들어갑니다.
2. **연동 설정** 버튼을 누릅니다.
3. **웹앱 URL**에 2단계에서 복사한 Apps Script 주소(`.../exec`)를 붙여넣습니다.
4. **토큰**에 1단계에서 정한 `SECRET_TOKEN` 값을 정확히 입력합니다.
5. **저장**을 누르면 "카드 문자 자동 동기화 켜짐"으로 바뀝니다. 이 설정은 이 아이폰의 이 앱에만 저장되며, 한 번만 하면 됩니다.

## 6단계. 신한카드 결제 문자 알림 켜기

신한카드 앱(또는 신한 SOL페이 앱)에서 결제 시 문자(SMS) 알림이 오도록 설정을 켜둡니다. 보통 **카드관리 → 이용알림 설정 → 문자(SMS) 알림** 경로에 있는데, 앱 버전에 따라 위치가 다를 수 있어 직접 찾아보셔야 합니다. 이미 켜져 있다면 건너뛰세요.

## 7단계. 아이폰 단축어 자동화 만들기

1. **단축어** 앱 → 하단 **자동화** 탭 → 오른쪽 위 **+** → **개인용 자동화 생성**.
2. **메시지**를 선택합니다.
3. **보낸 사람**에 신한카드 결제 문자가 오는 발신번호를 추가합니다. 본인 문자함에서 최근 결제 문자의 발신번호를 확인해서 넣어주세요.
4. (선택) "메시지에 다음 포함"에 `승인` 같은 단어를 넣으면 더 정확히 구분됩니다.
5. **다음** → **동작 추가** → **URL의 콘텐츠 가져오기** 검색해서 추가.
6. URL 칸에 2단계에서 복사한 Apps Script 웹앱 URL(`.../exec`)을 붙여넣습니다.
7. **방법**을 **POST**로, **요청 본문**을 **JSON**으로 바꿉니다.
8. 필드 두 개 추가:
   - `token` : 1단계에서 정한 SECRET_TOKEN 값 (직접 입력)
   - `text` : 마법 변수에서 **단축어 입력**(방금 받은 메시지 내용) 선택
9. 저장하면 완료입니다.

## 8단계. 테스트

1. 실제 결제를 하거나, 본인에게 "신한체크승인 1,000원 테스트 08/19 12:00" 같은 문자를 보내 흉내내봅니다.
2. 문자가 오면 자동화 알림 배너가 뜹니다 — **"실행"을 눌러야** 동작합니다. (아이폰은 완전 무클릭 자동화를 지원하지 않습니다.)
3. 홈 화면에 설치한 가계부 앱을 열고 확인함 탭에서 "지금 동기화"를 눌러보면 새 항목이 보여야 합니다.
4. 금액/가맹점이 이상하게 파싱되면, 실제로 받은 문자 원문을 캡처해서 알려주세요. `Code.gs`의 `parseCardSms_` 정규식을 그 형식에 맞게 다듬어드릴게요.

## 나중에 앱 화면(디자인/기능)을 수정하고 싶을 때

`index.html`을 수정한 새 버전을 받으면, GitHub 저장소 페이지에서 기존 `index.html`을 열고 연필(✏️) 아이콘 → 내용 전체 교체 → **Commit changes**만 하면 됩니다. GitHub Pages 주소는 그대로 유지되고 1분 안에 새 내용으로 바뀝니다. Apps Script처럼 "새 배포"를 따로 할 필요가 없습니다.

## 아이폰에서 데이터가 자꾸 사라진다면

홈 화면에 설치한 앱을 완전히 종료했다가 다시 켰을 때 입력한 내역이 사라지고 예전 상태로 돌아가 있다면, iOS가 "홈 화면에 추가"한 웹앱의 저장공간(localStorage)을 종료 시점에 안정적으로 반영하지 못하는 경우가 있어서입니다 — 여러 개발자들이 보고한 iOS/WebKit 자체의 알려진 동작이고, 앱 코드의 버그는 아닙니다. 이번 버전에서 두 가지를 보강했습니다.

- `manifest.json`을 추가해서 iOS가 이 앱을 더 "제대로 설치된" 웹앱으로 인식하도록 했습니다.
- 화면이 꺼지거나 앱이 백그라운드로 갈 때 저장을 한 번 더 강제로 시도하도록 했습니다.
- (이전에는) 저장공간이 비어있으면 앱이 조용히 예전 샘플(더미) 데이터를 다시 채워넣었는데, 이제는 저장공간이 비어있으면 그냥 진짜 빈 화면으로 시작합니다. 화면 구경이 필요하면 홈 화면의 "샘플 데이터로 둘러보기" 버튼을 눌러야만 나옵니다 — 그래야 "데이터가 사라졌다"는 사실을 안 헷갈리게 바로 알 수 있습니다.

그래도 데이터가 계속 사라진다면 iOS 자체의 저장공간 제약 때문일 가능성이 높습니다. 그 경우 근본적인 해법은 지금처럼 로컬 저장공간에만 의존하지 않고, 카드 결제 내역처럼 직접 입력한 지출·수입도 Apps Script + 구글 시트에 함께 저장해서 서버를 진짜 원본으로 쓰는 방식으로 바꾸는 것입니다 — 필요하시면 그렇게 확장해드릴 수 있어요.

## 참고 / 한계

- SECRET_TOKEN은 이 앱의 유일한 보안 장치입니다. Apps Script URL 자체도 추측하기 어려운 긴 문자열이라 실질적인 보호막이 되지만, 완전한 금융보안 등급은 아니라는 점은 참고해주세요. 계좌번호·비밀번호가 아니라 "결제 문자 내용"만 오가는 구조라 위험도는 낮습니다.
- 연동 설정(URL/토큰)은 이 아이폰의 이 앱(로컬 저장공간)에만 저장됩니다. 다른 기기에서 열면 다시 입력해야 합니다.
- 아이폰 자동화는 결제 즉시가 아니라 "문자를 받고 배너에서 실행을 누른 시점"에 반영됩니다.
- 여러 카드로 확장하고 싶으면 발신번호별로 단축어를 하나 더 만들고, `Code.gs`에서 발신번호나 문자 내용으로 카드사를 구분하는 로직을 추가하면 됩니다. 필요하시면 도와드릴게요.
- `gagyebu-app.html`을 컴퓨터에서 그냥 더블클릭해 열어도 데모 데이터로 미리보기가 가능합니다(연동 설정을 안 하면 자동으로 미리보기 모드로 동작). 실제로 쓰실 주소는 3단계에서 만든 GitHub Pages 주소입니다.
